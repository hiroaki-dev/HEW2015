<?xml version="1.0" encoding="utf-8"?>
<manifest package="me.hiroaki.hew"
		  xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/Theme.App">
		<activity android:name=".activity.LoginActivity"
				  android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity
			android:name=".activity.EventListActivity">
		</activity>
		<activity android:name=".activity.BoothListActivity">
		</activity>
		<activity android:name=".activity.BoothDetailActivity">
		</activity>
		<activity
			android:name=".activity.FeedBackActivity"
			android:theme="@style/Theme.App.Feedback">
		</activity>
	</application>

</manifest>
